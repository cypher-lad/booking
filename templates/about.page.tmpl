{{template "base" .}}

{{define "content"}}

<div class="container">
    <div class="row">
        <div class="col">
            <h1>This is the Aboutpage</h1>
            <p>This came from the template: {{index .StringMap "test"}}</p>

            {{if ne (index .StringMap "remote_ip") ""}}
                Your remote IP address is {{index .StringMap "remote_ip"}}
            {{else}}
                I don't know your remote IP yet. Please visit the <a href="/">Home Page</a> so I can set it.
            {{end}}

        </div>
    </div>
</div>

{{end}}